[project]
name = "local_tool"
version = "0.1.0"
description = "Artha local filesystem store and HTTP adapter"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "pydantic>=2.0",
    "blake3>=1.0",
]

